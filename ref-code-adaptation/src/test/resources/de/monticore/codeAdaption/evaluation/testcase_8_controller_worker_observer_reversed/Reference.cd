package de.monticore.codeAdaption.evaluation.testcase_8_controller_worker_observer_reversed;

import java.util.List;

classdiagram Reference {
  class Builder {
    any attribute;
    void setAttribute(any attribute);
  }

  class Subject {
    List<Observer> observers;
    boolean subscribe(Observer o);
    boolean unsubscribe(Observer o);
    void notifyAll();
  }

  interface Observer {
    void update();
  }
}





